export class Settings {
  cloud_host: string
  disabled: boolean;
  firebase_functions_host: string
  from_sms: string;
  to_sms: string;
  website_domain_name: string
  promo_codes: string[]
  projectId: string
  storage_keyfile: string
}
